spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/gym_crm_dev
    username: postgres
    password: postgres

  flyway:
    url: jdbc:postgresql://localhost:5433/gym_crm_dev
    user: postgres
    password: postgres
logging:
  level:
    root: error

